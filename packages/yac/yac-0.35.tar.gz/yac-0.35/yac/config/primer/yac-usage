
Your Atlassian Cloud (YAC)

usage: yac [options] <command> <subcommand> [<subcommand> ...] [parameters]
To see help text, you can run:

  yac -h
  yac <command> -h
  yac <command> <subcommand> -h

-h will provide api help at each level

Available commands include (in recommended order of attack!)

* vpc
* ssl
* stack
* db
* container
* app
* beer

New to YAC? 

In addition to api help, most of the commands support a -p parameter which will dump a "primer" doc to stdout.

For example ...

	get a vpc and vpc mapping primer using the vpc command

	get a stack and stack customization primer using the stack command
 

