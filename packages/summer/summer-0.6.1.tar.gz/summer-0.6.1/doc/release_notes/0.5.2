# -*- mode: text -*-
# Time-stamp: < 0.5.2 (2015-04-06 08:57) >

* migration to Python 3

* sources restructured to follow best practices of project layout

* source code formatting according to PEP 8

* broken compatibility in ldap
  removed base attribute in LDAP DAO session object (ie. self.session.base no longer works)
  added virtual attribute to LDAP DAO self.base instead of self.session.base
