{
  "name": "${name}",
  "description": "note description of your function",
  "region": "${region}",
  "handler": "${file_name}.${handler}",
  "role": "arn:aws:iam::00000000000:role/lambda_basic_execution",
  "timeout": ${timeout},
  "memory": ${memory}
}
