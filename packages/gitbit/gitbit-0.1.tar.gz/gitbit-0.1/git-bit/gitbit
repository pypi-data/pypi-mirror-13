#!/usr/bin/env python
import sys
import subprocess
k = sys.argv
l = len(k)
if(l == 1):
	subprocess.call("gitting")
elif(l == 2):
	subprocess.check_call(["gitting", str(k[1])])
elif(l == 3):
	subprocess.check_call(["gitting", str(k[1]), str(k[2])])
